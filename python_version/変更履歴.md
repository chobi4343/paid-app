# 変更履歴

## 2025年10月7日 - 絵文字削除アップデート

### 変更内容
全てのUI要素から絵文字を削除し、シンプルでプロフェッショナルな見た目にしました。

### 変更されたファイル

#### 1. `app_demo.py` (Streamlitデモ版)
- **削除した絵文字**:
  - タイトル: `📅` → なし
  - デモバッジ: `🧪` → なし
  - ボタン: `➕`, `⚙️`, `🔄`, `🏠`, `🗑️`, `💾`, `✏️`, `🆕` → 全て削除
  - 検索バー: `🔍` → なし
  - セクションタイトル: `📝` → なし

#### 2. `app_desktop_streamlit.py` (デスクトップ起動スクリプト)
- **削除した絵文字**:
  - 起動メッセージ: `📅`, `🚀`, `⏳`, `✅`, `🌐`, `📱`, `✨`, `💡`, `❌`, `👋` → 全て削除
  - ウィンドウタイトル: `📅` → なし

### 変更前
```
📅 有給休暇管理システム
🔍 検索: [_______]
➕ 新規登録  ⚙️ 設定
```

### 変更後
```
有給休暇管理システム
検索: [_______]
新規登録  設定
```

### メリット
✅ プロフェッショナルで落ち着いた見た目  
✅ ビジネス用途に適した印象  
✅ 読みやすさが向上  
✅ 環境によって絵文字が文字化けする問題を回避  

### 起動方法（変更なし）
```bash
cd "/Users/chobi/AI制作/有給管理/python_version"
python3 app_desktop_streamlit.py
```

---

## 機能一覧（変更なし）

全ての機能は従来通り使用できます：

- ✅ 従業員管理（登録・編集・削除）
- ✅ 有給休暇の付与・取得記録
- ✅ 法定付与の自動計算
- ✅ 残日数の自動計算（FIFO + 2年時効）
- ✅ 部署マスター管理
- ✅ 検索・フィルタリング機能

